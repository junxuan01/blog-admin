/**
 * Provider 统一导出
 *
 * 使用方式：
 * - 默认导入：import AppProvider from '@providers/index'
 * - 带地图版本：import { AppProviderWithMap } from '@providers/index'
 * - 主题配置：import { antdTheme } from '@providers/index'
 */

export { AppProviderWithMap, antdTheme, default } from './AppProvider';
